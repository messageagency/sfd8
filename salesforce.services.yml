services:
  salesforce.client:
    class: Drupal\salesforce\RestClient
    arguments: ['@http_client', '@config.factory', '@url_generator']

# services:
#   salesforce.soap_client:
#     class: Drupal\salesforce\SalesforceSoapClient
#     arguments: ['@http_client', '@config.factory', '@url_generator']
